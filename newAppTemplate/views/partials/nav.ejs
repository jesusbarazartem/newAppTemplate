<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-lg">
        <a class="navbar-brand" href="/"><i class="fa fa-dashcube fa-2x" aria-hidden="true"></i></a> 
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <% if (session && session.user) { %>
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link me-2" aria-current="page" href="/dashboard">Dashboard</a>
                    </li>
                </ul>
                <div class="d-grid gap-1 d-xl-flex mb-4 me-2 mb-xl-0">
                    <a href="/settings" class="btn btn-link disabled"><%=session.user.fullName%></a>
                    <a href="/settings" class="btn btn-primary"><i class="fa fa-user fa-lg" aria-hidden="true"></i></a>
                    <a href="/log-out" class="btn btn-danger"><i class="fa fa-sign-out fa-lg" aria-hidden="true"></i></a>
                </div>
                <% } else if (session && session.admin) { %>
                    <% } else { %>
                        <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                        </ul>
                        <div class="d-grid gap-1 d-xl-flex mb-4 me-2 mb-xl-0">
                            <a href="/log-in" class="btn btn-link">Login</a>
                            <a href="#" class="btn btn-primary">Contact Us</a>
                        </div>
                        <% } %>
        </div>
    </div>
</nav>